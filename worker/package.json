{
  "name": "linq-menu-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for LINQ Connect API",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
